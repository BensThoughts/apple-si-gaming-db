{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "db:migrate:deploy": {
      "outputs": []
    },
    "db:push": {
      "outputs": []
    },
    "db:seed": {
      "outputs": []
    },
    "db:stage": {
      "outputs": []
    },
    "dev": {
      "cache": false
    },
    "generate": {
      "dependsOn": ["^generate"]
    },
    "lint": {
      "outputs": []
    }
  },
  "globalDependencies": [
    "$ASGD_BANNER_SESSION_SECRET",
    "$ASGD_BATCH_SIZE",
    "$ASGD_DATA_DOWNLOAD_ATTEMPTED",
    "$ASGD_DAYS_SINCE_SYNC",
    "$ASGD_INITIAL_PAGE",
    "$ASGD_IS_LOCAL_CONTAINER",
    "$ASGD_PASSPORT_DOMAIN",
    "$ASGD_PASSPORT_SESSION_SECRET",
    "$ASGD_PROFILE_SESSION_SECRET",
    "$ASGD_STEAM_API_KEY",
    "$ASGD_THEME_SESSION_SECRET",
    "$DATABASE_URL",
    "$FLY_REGION",
    "$LOG_LEVEL",
    "$LOGGER_SERVICE_LABEL",
    "$LOGTAIL_SOURCE_TOKEN",
    "$NODE_ENV",
    "$PORT",
    "$PRIMARY_REGION"
  ]
}
